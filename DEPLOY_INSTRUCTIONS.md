# دليل بناء ونشر وإصلاح مشاكل الموقع (Deployment & Troubleshooting Guide)

هذا الملف مخصص لمساعدة أي مطور أو مستخدم يريد رفع وإدارة موقع **شركة النجمة لتصنيع البالتات الخشبية** على استضافة **Hostinger** ومعرفة المشاكل الشائعة وحلها بسرعة.

---

## 🚀 طريقة بناء ونشر الموقع (Build & Deployment Steps)

الموقع مبني باستخدام **Next.js** مع خاصية التصدير الثابت (`output: 'export'`).

### 1. بناء المشروع محلياً (Local Build)
لتوليد ملفات الموقع الثابتة (HTML/CSS/JS)، افتح سطر الأوامر في مجلد المشروع وشغّل:
```powershell
npm.cmd run build
```
*(ملاحظة: نستخدم `npm.cmd` لتفادي مشاكل صلاحيات تشغيل السكربتات في PowerShell على نظام Windows).*

بعد انتهاء البناء، سيتم إنتاج مجلد **`out`** الذي يحتوي على كل ملفات الموقع الثابتة والجاهزة للرفع.

---

### 2. طرق الرفع والنشر على استضافة Hostinger

#### 🛠️ الطريقة الأولى: الرفع التلقائي بالسكربت المدمج (الموصى بها)
لقد قمنا بإنشاء سكربت أوتوماتيكي يقوم بالاتصال بالـ FTP ورفع الملفات وفك ضغطها على سيرفر Hostinger بضغطة زر واحدة. 

**بيانات السيرفر المهيئة بالسكربت:**
* **Host (IP):** `194.36.184.34`
* **Username:** `u665169601.elnegmapallets.com`
* **Port:** `21`
* **Path:** `/public_html`

**خطوات التشغيل:**
1. أولاً، تأكد من ضغط محتويات مجلد `out` محلياً عبر تشغيل أداة الضغط المتوافقة مع لينكس:
   ```powershell
   node zip_out.cjs
   ```
   *(هذا الأمر ينشئ ملف `out.zip` متوافقاً تماماً مع السيرفرات).*
2. قم بتشغيل سكربت الرفع والنشر:
   ```powershell
   node ftp_clean_and_deploy.cjs
   ```
   سيقوم السكربت تلقائياً بمسح الكاش القديم، ورفع ملف `out.zip` ومعه سكربت فك الضغط `unzip.php` إلى سيرفر Hostinger، ثم يطلب فكه برمجياً ويمسح الملفات المضغوطة لتسريع العملية وحماية أمان الموقع.

---

#### 📦 الطريقة الثانية: الرفع اليدوي عبر لوحة تحكم Hostinger
إذا كنت تريد الرفع يدوياً:
1. قم بتشغيل أمر الضغط محلياً لتوليد ملف `out.zip`:
   ```powershell
   node zip_out.cjs
   ```
2. سجل الدخول إلى لوحة تحكم **Hostinger (hPanel)** ثم توجه إلى **File Manager** (مدير الملفات).
3. ادخل لمجلد **`public_html`**.
4. ارفع ملف `out.zip` فقط إلى المجلد.
5. اضغط بزر الفأرة الأيمن على الملف في مدير الملفات واختر **Extract** (فك الضغط).
6. احذف ملف `out.zip` بعد انتهاء العملية للمحافظة على نظافة المساحة وأمان السيرفر.

---

## ⚠️ المشاكل الشائعة وحلولها (Common Issues & Solutions)

### ❌ المشكلة الأولى: تشوه تصميم الموقع بعد الرفع عبر FTP
* **السبب:** الاستضافات المشتركة تضع حظر اتصال مؤقت (Rate Limit) عند رفع مئات الملفات الصغيرة متتالية عبر الـ FTP (كحزم الـ JS والـ CSS الخاصة بـ Next.js)، مما يؤدي لفشل رفع بعض ملفات التصميم وتشويه الموقع.
* **الحل:** استخدم **الطريقة الأولى أو الثانية** (الرفع كملف `zip` واحد ثم فكه على السيرفر) لأنها ترفع ملفاً واحداً فقط وتفكه في جزء من الثانية دون أي نسبة خطأ.

---

### ❌ المشكلة الثانية: روابط الصفحات تعطي خطأ 404 أو 403 عند تحديث الصفحة (Refresh)
* **السبب:** خوادم Apache لا تتعرف تلقائياً على مسارات Next.js وتطلب ملف `.html` مباشرة.
* **الحل الأول (مُطبّق حالياً):** تم تفعيل خيار `trailingSlash: true` في [next.config.mjs](file:///c:/Users/Islam%20Glab/Desktop/elnegmapallets/next.config.mjs). هذا يجعل Next.js يُصدر الصفحات كـ مجلدات تحتوي على `index.html` (مثال: `/gallery/index.html` بدلاً من `/gallery.html`)، وهو ما يفهمه خادم Apache تلقائياً ويعمل دون مشاكل.
* **الحل الثاني:** استخدام ملف [.htaccess](file:///c:/Users/Islam%20Glab/Desktop/elnegmapallets/public/.htaccess) لتوجيه المسارات بشكل نظيف. الملف مهيأ حالياً بالصيغة التالية:
  ```apache
  RewriteEngine On
  RewriteBase /
  
  # عدم توجيه ملف إثبات ملكية جوجل
  RewriteRule ^google[a-z0-9]+\.html$ - [L]
  
  # عدم توجيه الملفات أو المجلدات التي لها وجود حقيقي على السيرفر
  RewriteRule ^index\.html$ - [L]
  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteCond %{REQUEST_FILENAME} !-d
  RewriteRule . /index.html [L]
  ```

---

### ❌ المشكلة الثالثة: إثبات ملكية جوجل سيرش كونسول (Google Search Console)
* **السبب:** جوجل يحتاج للتحقق من ملف ملكية مثل `google70cdcc5e9cf2fa58.html` في المجلد الرئيسي.
* **الحل:** تم وضع هذا الملف داخل مجلد [public/google70cdcc5e9cf2fa58.html](file:///c:/Users/Islam%20Glab/Desktop/elnegmapallets/public/google70cdcc5e9cf2fa58.html). عند عمل أي بناء (Build) للموقع، سينتقل الملف تلقائياً إلى مجلد `out` الرئيسي ليتم رفعه مباشرة دون الحاجة لرفعه يدوياً في كل مرة.
