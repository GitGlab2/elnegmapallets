# دليل بناء ونشر الموقع على Hostinger 🚀

تم إعداد أدوات النشر التلقائي للموقع لتبسيط عملية الرفع وضمان سلامة الملفات على استضافة Hostinger.

## ⚙️ الخطوة الأولى: إعداد كلمة مرور الـ FTP
1. قم بإنشاء ملف جديد في المجلد الرئيسي للمشروع باسم **`.env.local`**.
2. أضف السطر التالي بداخل الملف واستبدل كلمة المرور بكلمة مرور الـ FTP الخاصة بك على Hostinger:
   ```env
   FTP_PASSWORD=كلمة_مرور_الـ_FTP_الخاصة_بك
   ```
*(ملاحظة: هذا الملف يتم تجاهله تلقائياً بواسطة git وهو آمن تماماً ولا يتم رفعه على مستودع GitHub).*

---

## 🛠️ الخطوة الثانية: بناء وتعبئة الموقع (Build & Package)
لتوليد الملفات الثابتة وضغطها في ملف `out.zip`:
1. قم ببناء الموقع:
   ```bash
   npm run build
   ```
2. قم بتشغيل سكربت الضغط:
   ```bash
   node zip_out.cjs
   ```
   سيتم توليد ملف باسم `out.zip` في المجلد الرئيسي للموقع.

---

## 🚢 الخطوة الثالثة: النشر التلقائي (Deploy)
قم بتشغيل سكربت الرفع التلقائي:
```bash
node ftp_clean_and_deploy.cjs
```

**ماذا يفعل هذا السكربت تلقائياً؟**
1. يتصل بسيرفر Hostinger عبر الـ FTP.
2. يرفع ملف `unzip.php` المسؤول عن فك الضغط.
3. يرفع ملف `out.zip` الذي يحتوي على الموقع الجديد.
4. يطلب من السيرفر فك الضغط برمجياً لتحديث الموقع في أقل من ثانيتين وبشكل آمن تماماً.
