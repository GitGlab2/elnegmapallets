# 📌 PROJECT HANDOVER & STATUS — وثيقة تسليم واستمرارية المشروع

> [!IMPORTANT]
> **إلى النموذج القادم (To the incoming AI agent):**
> اقرأ هذا الملف **أولاً وبشكل كامل** قبل اتخاذ أي خطوة أو كتابة أي كود. هذا الملف يحتوي على القوانين الثابتة للمشروع، هيكلية الكود، ما تم إنجازه، وما يجب القيام به بعد ذلك.

---

## 1. ⚙️ معلومات أساسية عن المشروع (Project Core Specs)

- **نوع المشروع:** تطبيق Next.js (App Router) مع تصدير ثابت (Static HTML Export) إلى مجلد `out/`.
- **رابط الموقع الرسمي:** `https://elnegmapallets.com`
- **اللغات المدعومة:** 
  - العربية (المسار الافتراضي عند الجذور مثل `/` و `/articles`).
  - الإنجليزية (المسار يبدأ بـ `/en` مثل `/en/` و `/en/articles`).
- **المستودع الرئيسي للملفات والبيانات:**
  - المقالات العربية: [articles.ts](./client/src/data/articles.ts) (والملفات الفردية في المجلد [articles](./client/src/data/articles)).
  - المقالات الإنجليزية: [articles-en.ts](./client/src/data/articles-en.ts).
  - بيانات مقاسات البالتات: [pallet-sizes.ts](./client/src/data/pallet-sizes.ts) (عربي) و [pallet-sizes-en.ts](./client/src/data/pallet-sizes-en.ts) (إنجليزي).
  - مولد المقالات التلقائي للمقاسات: [pallet-size-articles.ts](./client/src/data/pallet-size-articles.ts).

---

## 2. 🛡️ التكليفات الثابتة وقوانين الجودة (Strict Rules & Mandates)

المستخدم هو **المشرف ومدير الجودة والاستراتيجية**. يجب الالتزام الصارم بالقوانين التالية:

1. **لا تقبل عبارة "تم التنفيذ بنجاح" دون إرفاق التقرير الثماني التالي:**
   - الملفات المعدلة بدقة.
   - ملخص دقيق للتغييرات البرمجية واللوجستية.
   - نتائج البناء والتصدير الفعلي (`npm run build`).
   - روابط الصفحات المتأثرة.
   - تقرير المعاينة والتجاوب مع الهاتف والكمبيوتر.
   - التحذيرات وأي نقاط ضعف مكتشفة.
   - الأثر الإيجابي أو السلبي على الـ SEO.
   - تأكيد قطعي بخلو الموقع من صفحات الخطأ 404.
2. **النظافة التامة (Strict Code Cleanliness):**
   - ممنوع تماماً ترك أي ملفات تخطيط، مسودات، نصوص غير مستخدمة، أو ملفات قديمة في جذر المشروع.
   - ممنوع استخدام أطر عمل SPA أو Vite أو Express أو باتشات قديمة.
3. **دقة المصطلحات والبيانات اللوجستية:**
   - اسم الكيان: **مصنع النجمة لتصنيع البالتات الخشبية** (El Negma Pallets).
   - المقر الفعلي: **بياض العرب، بني سويف، مصر**.
   - نوع المنتجات: **بالتات خشبية جديدة آلية** (وليس بالتات مستعملة كمنتج أساسي).
   - معيار المعالجة: **المعالجة الحرارية HT المعتمدة بختم ISPM-15**.

---

## 3. 🚀 ما تم إنجازه في الجلسات الأخيرة (Accomplished Tasks)

تم تنفيذ إصلاحات برمجية وهيكلية هامة لضبط جودة الـ SEO وجوانب البناء والأداء دون أي أخطاء:

1. **طبقة التهيئة المركزية وديناميكية الـ Sitemap:**
   - تم إنشاء [site-config.ts](./client/src/lib/site-config.ts) يحتوي على ثوابت النطاق ودالة بناء روابط canonical آمنة وموحدة تضمن إغلاق المسارات بشرطة مائلة (`/`).
   - تم حذف Sitemap اليدوية واستبدالها بمولد ديناميكي [sitemap.ts](./app/sitemap.ts) يربط مسارات المقالات والبالتات برمجياً بصورة متوافقة مع التصدير الثابت لـ Next.js.
2. **إصلاح الروابط المتعددة (Hreflang) و canonical:**
   - التحقق البرمجي من تواجد الترجمات المقابلة قبل حقن وسوم `hreflang` لمنع أخطاء 404 للغات غير المتوفرة (مثل مقال أبعاد البالتات القياسية غير المترجم للإنجليزية).
   - منع وراثة canonical الصفحة الرئيسية من قبل الصفحات الداخلية عبر توفير canonical ذاتي معتمد لكل صفحة ومسار.
3. **إعادة هيكلة البيانات المنظمة (Structured Data):**
   - إزالة `FAQPage` Schema المخفية من الـ Layouts العامة لتفادي عقوبات جوجل الترتيبية.
   - ربط مخطط المقالات (Article Schema) بالناشر عبر معرف الشركة الموحد (`#organization`).
4. **تحسين أداء الخطوط وعرض المقالات:**
   - إزالة أوزان الخطوط Cairo و Outfit غير المستخدمة (300 و 800) لتقليل حجم الطلبات وتحسين مؤشر سرعة تحميل الصفحات.
   - إخفاء جدول محتويات المقالات (TOC) تلقائياً في حال غيابه من المقال لتجنب الروابط المكسورة أو الفارغة.
5. **إدراج فحوصات الجودة المؤتمتة:**
   - إدراج أوامر `typecheck`, `validate:build` وأمر `quality` لتشغيل الفحوصات والتحقق البرمجي وسحب وبناء وفحص المشروع ذاتياً.
   - بناء سكربت فحص ما بعد البناء [validate-build.js](./scripts/validate-build.js) للتحقق التلقائي من خلو مجلد `out` من أي أخطاء canonical أو hreflang أو روابط مكسورة أو h1 مكرر.

---

## 4. 📝 الخطوات القادمة الموصى بها (Next Steps)

- **الحفاظ على تشغيل Pipeline الفحص:** يوصى دائماً بتشغيل `npm run quality` قبل دفع أي كود للتحقق من سلامة البناء وتناسق مسارات الـ SEO تلقائياً.
- **متابعة المحتوى والمقالات الجديدة:** الاستمرار في التنسيق المتفق عليه لنمط عرض قائمة المقالات والالتزام بحدود أحجام الصور.
